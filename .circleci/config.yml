version: 2

jobs:
  build:
    docker:
      - image: cahirwpz/demoscene:latest

    working_directory: ~/demoscene

    steps:
      - checkout
      - run: 'git lfs install'
      - run: 'cd a500 && make'
